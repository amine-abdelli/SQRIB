## CLAUD Rules

- **Ignore `/_legacy`**: This folder contains deprecated / historical code and assets.  
  - Do **not** read, edit, refactor, move, or rely on anything inside `/_legacy`.  
  - When searching the codebase or making changes, treat `/_legacy` as excluded.

All active and maintainable code lives outside of `/_legacy`.
