export interface IStats {
  wrongWords: number,
  wordCount: number,
  correctLetters: number,
  totalLetters: number,
  wrongLetters: number,
  precision: number,
  points: number,
  mpm: number,
  correctWords: string[],
}
