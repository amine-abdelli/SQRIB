export enum AUTH_ROUTES {
  LOGIN = '/login',
}

export enum MAIN_ROUTES {
  TRAINING = '/training',
  PRACTICE = '/practice',
  MULTIPLAYER = '/multiplayer',
  LEADERBOARD = '/leaderboard',
  SETTINGS = '/settings',
}
