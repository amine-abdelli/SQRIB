export enum AUTH_ROUTES {
  LOGIN = '/login',
}

export enum MAIN_ROUTES {
  TRAINING = '/training',
  LEARNING = '/learning',
  MULTIPLAYER = '/multiplayer',
  LEADERBOARD = '/leaderboard',
  SETTINGS = '/settings',
}
