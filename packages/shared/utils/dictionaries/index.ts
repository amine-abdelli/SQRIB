import fr from './fr/fr';
import en from './en/en';
import de from './de/de';
import es from './es/es';
import { TLanguage } from '../../types';

export const dictionaries: Record<TLanguage, string[]> = {
  fr, de, en, es,
};
