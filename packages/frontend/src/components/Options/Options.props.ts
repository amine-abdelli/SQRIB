export interface ISettingsProps {
  language: string;
  fontSize: number;
  theme: boolean;
  sound: boolean;
}

export interface OptionsProps {
  sound?: boolean,
  fontSize?: boolean,
  language?: boolean
}
